<%= form_for @request, url: requests_path do |f| %>
  <%= f.hidden_field :recipient_id, value: freelancer.user_id %>
  <%= f.hidden_field :title, value: @project.first.title %>
  <%= f.hidden_field :description, value: @project.first.description %>
  <%= f.submit "Request Consultation" %>
<% end %>
