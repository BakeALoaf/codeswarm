<h1><%= @freelancer.first_name %> <%= @freelancer.last_name %> Profile</h1>

<p><strong>Name:</strong> <%= @freelancer.first_name %> <%= @freelancer.last_name %></p>

<p><strong>Skill:</strong> <%= @freelancer.skill %></p>

<p><strong>Portfolio:</strong> <%= @freelancer.portfolio %></p>

<p><strong>Pricing:</strong> <%= @freelancer.pricing %></p>

<%= link_to "Edit Profile", edit_freelancespec_path(@freelancer) %>
<%= link_to "Delete Profile", freelancespec_path(@freelancer), method: :delete, data: { confirm: "Are you sure?" } %>

<%= render partial: 'requests/form', locals: { request: @request } %>
